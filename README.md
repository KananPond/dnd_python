# dnd — PLATO(1975)现代复刻

1975 年运行在 PLATO IV 系统上的地牢爬行游戏 **dnd**（Gary Whisenhunt & Ray Wood）的现代重制。
**不做 1:1 还原**：机制骨架忠实（L1）＋形式现代化（L2）＋内容再创作（L3），每条内容都在数据表里
标了可信度分级，考据过程见 [`docs/research-dossier.md`](docs/research-dossier.md)。

| 项 | 值 |
|---|---|
| 前端 | Python 标准库 **curses**（零第三方依赖，没装 pip 也能跑） |
| 内核 | 自研确定性内核：seeded PRNG，内核内不使用 `random` / `time`，TUI / 机器人 / 回放测试共用同一份逻辑 |
| 平台 | Linux / macOS / Windows 上的 **WSL2**（原生 Windows Python 没有 `curses`，不支持） |
| Python | ≥ 3.10（本轮实测于 3.14.4） |
| 规模 | 游戏本体 **4093 行** Python（其中界面 2161 行）＋工具 872 行＋测试 1641 行 |
| 验证 | **123 项**单元测试（内核 37 / TUI 76 / CLI 10）、**44 项** tmux 伪终端冒烟、18 个界面预览场景，本轮全部通过 |

> 📖 **第一次玩？看 [`docs/how-to-play.md`](docs/how-to-play.md)**（界面图解、键位总表、生存要点、FAQ）。
> 📐 **想改代码/看设计？** [`docs/reimplementation-plan.md`](docs/reimplementation-plan.md)（技术选型、目录、保真度矩阵、里程碑、决策留痕）。
> 🔍 **想核对出处？** [`docs/research-dossier.md`](docs/research-dossier.md)（PLATO《dnd》史料、A/B/C 可信度分级、以及背景故事层的「被遗忘的国度」母题调研与改造对照）。
> 🌍 **想读设定？** [`docs/world-setting.md`](docs/world-setting.md)（完整世界观：诸神、种族、职业、深渊遗物与时间线）。
> ⚖️ **关心许可/合规？** [`docs/open-source-compliance.md`](docs/open-source-compliance.md)（商标、素材、依赖、隐私逐项证据）。

## 快速开始

```bash
./pydnd.sh                                        # 唯一推荐入口：Linux / macOS / WSL2 通用
./pydnd.sh --check                                # 只做环境自检并打印报告，不启动游戏（报 bug 时请附上它）
python3 -m dnd                                    # 等价入口：脚本只做前置校验，不碰游戏逻辑
```

想在**任何目录**直接敲 `pydnd` 启动？装一个全局命令——用两行包装脚本把仓库绝对路径写死
（直接软链 `pydnd.sh` 不行：脚本刻意不做符号链接解析，见脚本头部注释）：

```bash
mkdir -p ~/.local/bin
printf '#!/bin/sh\nexec <仓库绝对路径>/pydnd.sh "$@"\n' > ~/.local/bin/pydnd
chmod +x ~/.local/bin/pydnd
```

`~/.local/bin` 在 Ubuntu/Debian 的默认 `~/.profile` 里已进 PATH，新开终端即可用；之后 `git pull`
更新仓库无需重装，只有移动仓库位置时才需要改包装脚本里的那一行路径。

启动后是**开始页**：`新的冒险 / 读取存档 / 名人堂 / 退出游戏`，**↑↓** 移动、**回车**确认（也可 **1-4** 直选、**Esc** 退出）。

- **新的冒险** → 建角：输入姓名 → ↑↓ 选职业 → `Tab` / `Shift+Tab` / `←→` 切到种族栏 → ↑↓ 选种族 → 回车开始
- 建角之后先读**序章**：只有 2 页（**世界背景 → 入井**），←→ 翻页、回车读完进场、`Esc` 跳过；进游戏后按 **`B`** 随时重看，`--no-prologue` 可跳过
- **读取存档** → 存档管理：↑↓ 选、回车读、`D` 删（确认框默认停在「取消」），不需要输任何命令
- 游戏内按 **Esc** 打开暂停菜单：继续 / 保存进度 / 存档管理 / 返回开始页 / 退出

命令行参数是"老玩家快捷键"（也是伪终端冒烟测试依赖的路径）：

```bash
./pydnd.sh --seed 42 --name Aria --class wizard --race elf   # 三个都给出才跳过开始页直接开局（仍会过一次序章）
./pydnd.sh --no-prologue                           # 跳过建角后的背景故事，直接进地牢
./pydnd.sh --lore --name Aria                      # 不上界面，直接在终端里打印序章
./pydnd.sh --load saves/aria.json                  # 直接读指定存档
./pydnd.sh --latest                                # 读最近一次存档（跳过开始页）
./pydnd.sh --menu                                  # 参数再全也先回开始页
./pydnd.sh --modern                                # 现代模式：免死一次
./pydnd.sh --debug                                 # 调试键：x 显示全图，t 传送
./pydnd.sh --no-color                              # 关闭颜色
./pydnd.sh --ascii                                 # 框线/进度条退回纯 ASCII（中文终端把 ─│ 当两格宽时用）
./pydnd.sh --list-saves                            # 列出存档文件（脚本用）
./pydnd.sh --roster                                # 打印名人堂
./pydnd.sh --headless-demo 300 --seed 5            # 无终端环境自检：跑 300 回合并打印状态
```

### 平台支持

| 系统 | 怎么跑 | 说明 |
|---|---|---|
| **Linux** | `./pydnd.sh` | 需要 `python3`（≥ 3.10）。发行版若未自带：`sudo apt install -y python3` |
| **macOS** | `./pydnd.sh` | 系统自带 `python3` 可能只有 3.9；用 `brew install python` 装新的，或 `DND_PYTHON=/opt/homebrew/bin/python3 ./pydnd.sh` |
| **Windows** | **必须用 WSL2** | WSL 终端里 `cd` 到仓库后 `./pydnd.sh`。原生 cmd/PowerShell/Git Bash **不支持**（原生 Python 没有标准库 `curses`）。安装：管理员 PowerShell 执行 `wsl --install -d Ubuntu`，重启后进入 `wsl` |

`pydnd.sh` 的校验顺序固定为：平台（原生 Windows 直接给 WSL2 安装指引）→ Python 版本与 `curses` →
是否真实终端 → 终端编码（不是 UTF-8 且系统有可用 UTF-8 locale 时自动补上）→ 窗口尺寸，然后才 `exec python3 -m dnd`。
任何一步不过都给出可操作的中英文提示，**不会留下一个意义不明的 traceback**；自检失败时退出码为 2。

环境：Python ≥ 3.10（开发机 3.14）、支持 curses 的终端。建议窗口 **≥ 100×30**（宽度 ≥ 100 时右侧显示**冒险者档案**：
生命/法力/经验条、视野内敌人、装备、属性、背包）；最小可运行尺寸 **62×18**，再小会显示"终端窗口太小"。

## 操作

| 键 | 作用 | 键 | 作用 |
|---|---|---|---|
| **W A S D** / 方向键 / `hjkl` / `yubn` | 移动（`yubn` 为斜向） | `.` 或 `5` 或空格 | 原地等待一回合 |
| `>` | 站在下楼梯上时下潜 | `<` | 上楼梯回上一层 |
| `g` 或 `,` | 拾取脚下物品（金币自动入袋） | `i` | 背包（按字母使用/装备/喝/读） |
| `c` | 施法（按字母选择） | `?` | 帮助页（含考据说明），任意键关闭 |
| `B` | **重看序章 / 背景故事**（与建角后同一个界面） | `F5` 或 `Shift+S` | 存档 |
| `R` | 名人堂名册 | `P` | 切换侧栏出处标记 |
| `Q` | 退出（确认框默认停在「取消」） | `Esc` | **暂停菜单**（继续/保存/存档管理/回开始页/退出） |
| `x` / `t` | 仅 `--debug`：显示全图 / 传送到某层 | | |

> 所有菜单都能**只用方向键 + 回车**走完：开始页、存档管理、暂停菜单、建角、序章、确认框，一个不落。
> 危险操作（退出、返回开始页、删存档）的确认框**默认停在「取消」**，按 `←→` 换选项。

## 玩法

开始页 → 建角（4 职业 × 4 种族）→ 读**序章（背景故事）** → 潜入 **10 层**地牢 → 探索、战斗、拾宝、升级 →
第 10 层夺取**深渊遗物**即胜利（守卫就守在遗物旁，**拿到即赢，不必杀死它**）。
死亡是**永久**的：角色写进名人堂（`saves/roster.json`），存档被删除；`--modern` 可免死一次。

> **序章**：建角之后先读这一局的背景故事，**只有两页**——**世界**（这一切是怎么来的）与
> **入井**（深渊之门与逐光者是什么、你怎么下去，以及下井规矩：十层 / 深渊遗物 / 深渊守卫 / 死亡永久）。
> ←→ 翻页、回车读到底进场、`Esc` 跳过；进游戏后按 **`B`** 重看，`--no-prologue` 跳过，
> `--lore` 在终端里打印纯文本版。序章刻意只写"进场必须知道的东西"，**完整设定**（诸神 / 种族 / 职业 /
> 遗物 / 时间线）在 [`docs/world-setting.md`](docs/world-setting.md)；母题调研见研究档案第八节。

| 机制 | 数值（全部来自 `dnd/data/*.json`，可整体替换） |
|---|---|
| 命中判定 | d20 + 命中加值 ≥ 目标 AC；**掷出 20 暴击（双倍伤害），掷出 1 必定失手** |
| 护甲 | AC = 10 + 护甲加值 + 敏捷调整值（皮甲 +2 / 镶钉 +3 / 锁子甲 +5 / 板甲 +7）+ 法术增益 |
| 视野 | 递归阴影投射，基础半径 **9**（精灵/侏儒 +1）＋记忆雾；怪物**仇恨半径 7**，超出不再穷追 |
| 生命恢复 | 每 **12 回合 +1 HP**；治疗药水 1d8+2，强效 2d8+4，牧师可施「治疗轻伤」 |
| 升级 | 经验达阈值升级（15 / 45 / 100 / 190 / 320 …，之后每级 +1500）：加生命骰 + 体质调整，施法职业同时加 MP |
| 掉落 | 金币、宝石、武器、护甲、药水、卷轴，按深度加权分层 |

**4 种族**（建角预览框实时显示合成数值）：人类（生命/法力各 +1）、精灵（视野 +1、法力 +1，敏捷 +2、体质 −1）、
矮人（生命 +2，体质 +2、敏捷 −1）、侏儒（法力 +2、视野 +1，智力 +1、敏捷 +1、力量 −1）。
**4 职业**：战士（d10 生命骰，无法术）、法师（d6，魔法飞弹/护盾/闪电束）、牧师（d8，治疗轻伤/祝福，对不死生物命中 +2）、盗贼（d8，无法术）。

法术表共 5 个法术（法师 3 / 牧师 2），另有 2 种伤害卷轴；12 种怪物按深度 1–10 加权分布，18 种物品。金币目前只作计分。

## 数据与考据

原版 dnd 的职业、法术、怪物、数值**均已不可考**（见研究档案第四节"未确证清单"）。因此：

| 层 | 内容 | 说明 |
|---|---|---|
| **L1 忠实** | 单屏字符地牢、俯视探索、逐层下潜、掷骰战斗、角色持久化、永久死亡、宝物驱动成长 | 有公开史料支撑的骨架 |
| **L2 现代化** | 键位与帮助、视野与记忆雾、仇恨半径、生命自然恢复、上升式 AC、存档/名册、ASCII/无颜色降级 | 现代可玩性所需 |
| **L3 再创作** | 4 职业、4 种族、5 法术、12 怪物、18 物品与各自数值、10 层难度曲线、遗物结局，以及整套世界观与序章 | 43 条内容 ＋ 1 份序章文本全部标了 `confidence` 与 `_meta`；完整世界观见 `docs/world-setting.md` |

游戏内按 `?` 可看到同样的分级说明；`P` 在侧栏显示出处标记。
六张数据表的 `_meta` 里同时标了 `"license": "CC0-1.0"`——内容是公共领域的，代码是 MIT。
序章文本（`dnd/data/lore.json`）也在其中：换掉它就能改游戏内序章，完整设定改 `docs/world-setting.md`，两处都不用动代码。

## 验证（可重复执行，本轮全绿）

```bash
./pydnd.sh --check                              # 环境自检：平台/Python/curses/终端编码/尺寸……一次全打印
python3 -m unittest discover -s tests -v       # 123 项：内核 37 / TUI 76 / CLI 10
python3 tools/pty_smoke.py                     # 44 项：tmux 真实伪终端里的端到端交互冒烟
python3 -m dnd --lore                          # 背景故事纯文本版（无终端也能跑）
python3 -m dnd --headless-demo 300 --seed 5    # 无终端环境自检（跑 300 回合）
python3 tools/sim.py --runs 40 --class warrior # 贪心机器人跑批统计（--json out/sim.json 可落盘）
python3 tools/preview.py                       # 把界面渲染成 PNG 供人工审阅（需要 Pillow，仅开发用）
```

| 测试文件 | 覆盖 |
|---|---|
| `tests/test_core.py`（37） | RNG 确定性与序列化、掷骰解析、地图生成与连通性、第 10 层守卫/遗物布局、FOV、战斗与经验、上下楼、死亡永久性、存档往返与 `_meta`、旧档/坏档容错、回放一致性、种族数值、**序章文本**（两页结构、下井须知、点名角色、不含他方专有名词） |
| `tests/test_tui.py`（76） | 假 curses 屏幕渲染：开始页光标与反白、存档管理列表/详情/删除确认、暂停菜单、建角两栏焦点、**序章翻页与边框完整、`B` 重看背景**、中文按显示宽度折行与裁剪、面板几何回归、Unicode/ASCII 字形切换、说明文案 |
| `tests/test_cli.py`（10） | `--list-saves`、`--name/--class/--race` 缺项进建角、参数齐备直接开局、**建角后展示序章、`--no-prologue` 直进地牢、`--lore` 文本输出**、`--load` 在真实伪终端里进入游戏、读不存在的存档报错 |

`tools/preview.py` 的 18 个场景：开始页 / 开始页矮窗口 / 存档管理 / 存档管理空状态 / 删除确认 / 建角 /
**序章（背景故事）** / 主界面 / 背包 / 法术 / 帮助 / 名人堂 / 阵亡结算 / 暂停菜单 / 最小尺寸 / 窄窗口 / 过小窗口 / ASCII 兜底。

改界面的工作流：`test_tui.py` 断言**布局关系**（谁压谁、有没有越界），`tools/preview.py` 给出**观感**。

**平衡现状**（贪心机器人，人类种族，非现代模式，seed 1–40 各 40 局，回合上限 4000，本轮实测）：

| 职业 | 平均深度 | 最高深度 | 平均等级 | 主要死因 |
|---|---|---|---|---|
| 战士 | 4.50 | 6 | 3.60 | 窖犬、骷髅 |
| 盗贼 | 3.30 | 6 | 2.60 | 窖犬、灰壤鼠 |
| 牧师 | 3.12 | 6 | 2.35 | 窖犬、灰壤鼠 |
| 法师 | 2.12 | 5 | 1.50 | 窖犬、灰壤鼠 |

40 局里只有极少数因到达回合上限而"未结束"，其余全部阵亡；无人通关。**法师偏弱是已知项**，属于调参工作。

## 目录结构

```
pydnd.sh               跨平台启动脚本（POSIX sh：Linux / macOS / WSL2；环境自检 + 启动，含 --check）
pyproject.toml         包元数据（MIT、requires-python ≥ 3.10、可选依赖 Pillow、console script 入口）
LICENSE                代码协议（MIT）｜ dnd/data/LICENSE-CC0.txt 数据协议（CC0-1.0）
dnd/                   游戏包（python3 -m dnd）
  __main__.py          命令行入口与「开始页 ↔ 建角 ↔ 序章 ↔ 地牢」主循环（延迟导入 UI）
  rng.py               确定性 PRNG（xoshiro256** + splitmix64，状态可 JSON 序列化）
  dice.py              掷骰表达式（'2d6+1' / 'd8' / '3'）
  content.py           JSON 数据表加载与索引（按深度加权筛选怪物/物品）
  lore.py              序章装配（lore.json → 2 页；不 import curses，无终端也能用）
  data/*.json          职业 4 / 种族 4 / 怪物 12 / 物品 18 / 法术 5 / 背景故事 1（均带 _meta 出处与可信度）
  level.py             地图生成（48×26 房间+走廊、门、楼梯、分层放置、BFS 连通性校验）
  fov.py               视野（递归阴影投射，8 个八分圆）
  entities.py          玩家 / 怪物 / 物品、建角、自动装备
  game.py              内核状态机（回合、战斗、法术、物品、升级、胜负、状态哈希）
  save.py              存档（原子写 + `_meta` 摘要）、存档索引/删除、名人堂名册
  replay.py            确定性回放（seed + 命令序列 → 状态哈希）
  ui/theme.py          配色与字形（PLATO 琥珀观感；真彩/256/8 色三级降级 + Unicode/ASCII 字形集）
  ui/widgets.py        绘制辅助（框线/分隔线/进度条/按键提示；按显示宽度裁剪中文）
  ui/describe.py       物品/法术说明（内部 effect id → 玩家看得懂的一句话）
  ui/screens.py        开始页 / 存档管理 / 建角 / 序章（↑↓ 驱动的开局四屏，面板排版只在这里算一次）
  ui/tui.py            curses 主界面与浮层（游戏内：地图、侧栏、日志、暂停菜单、结算）
tools/sim.py           无头贪心机器人跑批（平衡性与稳定性）
tools/pty_smoke.py     真实伪终端（tmux）里的 TUI 冒烟测试
tools/preview.py       把界面渲染成 PNG 供人工审阅（需要 Pillow，仅开发用）
tests/                 内核 / TUI 布局 / CLI 三条测试线
docs/                  研究档案、世界观设定集、复刻计划、上手指南、开源合规
saves/                 存档与 roster.json（存档不入库，只保留 .gitkeep）
out/                   机器人跑批与界面预览输出（gitignore）
```

## 存档与复现

- 存档是**纯 JSON**（角色 + 地牢 + RNG 状态），写在仓库的 `saves/` 下，按角色名一人一档。
- 顶层多一个 `_meta` 摘要字段（名字/职业/种族/等级/深度/回合/金币/生命/时间），**存档管理界面**直接用它显示，
  不必为了列一行摘要反序列化整份地牢；旧存档没有 `_meta` 也能列出，界面会退回文件名与修改时间。
- 写入是**原子**的（先写 `.tmp` 再 `os.replace`），不会因为写一半崩溃把存档写坏。
- 损坏的 JSON 仍会列出来并标成「损坏」，可以在界面里选中按 `D` 删掉，不需要回命令行。
- 结局画面会显示**种子**（如 `种子 12345`），`python3 -m dnd --seed 12345` 就能重开同一座地牢。
- 内核确定性：同一 `--seed` ＋ 同一操作序列，跨进程得到完全一致的状态哈希（`Game.state_hash()`）；
  本局执行过的命令（含 `--debug` 的 `x`/`t`）都会完整记进 `Game.commands`，`replay.run(seed, commands)` 能原样复现。

## 已知问题 / 路线图

- **平衡**：见上文「验证」一节的四职业跑批；法师偏弱、无人通关，属于调参工作（也可通过增强机器人策略验证）。
- **终局**：深渊守卫（AC 19 / 10d8+20 / +11 3d8+3）正面硬拼几乎打不过，设计意图是"引开它、再冲刺抢遗物"。
  守卫必定生成，遗物就放在它**身边**（不压在它脚下，否则等于必须先杀掉打不过的 boss 才能通关）。
- 金币目前只作计分，尚无商店/祭坛等消耗途径。
- 尚未实现：多角色槽位（仍是一人一档）、存档导入导出、通关回程（带遗物返回地面）、洞穴型/主题化层、
  陷阱与门锁、音效、i18n、分享串。
- 存档目录固定为仓库内 `saves/`，尚无 `DND_SAVE_DIR` 之类的位置覆盖。
- 尚未核实：原版的职业/法术/层数/胜利条件（拿到资料后只需替换 `dnd/data/*.json`，无需改代码）。
- **背景故事**：整套世界观是 L3 自创（原版可考的剧情为零，见研究档案第八节），**不与任何商业设定共享名词**；
  游戏内序章只讲"世界背景 + 怎么进地牢 + 下井须知"，诸神/种族/职业/遗物等细设定都在
  `docs/world-setting.md`。序章目前只有中文，i18n 未做；想换设定改 `dnd/data/lore.json`（游戏内）与
  `docs/world-setting.md`（完整设定），都不用动代码。
- 开发注意：`ui/theme.py` 在模块级 `import curses`，新增 UI 引用请照 `__main__.py` 的写法**延迟导入**，
  否则 `--headless-demo` / `--list-saves` 在没有 curses 的环境（Windows 原生 Python）会直接崩。

## 远端仓库

本仓库同时发布在两个平台，内容一致，**两边都只保留一个分支 `master`**（本地分支也叫 `master`）：

| 平台 | 地址 | 分支 |
|---|---|---|
| Gitee（主） | https://gitee.com/shenzhenshiguanfminquxinhujiedao/dnd_python | `master` |
| GitHub | https://github.com/KananPond/dnd_python | `master` |

```bash
git remote -v          # gitee / github 两个远端
git push gitee master   # 推 Gitee
git push github master  # 推 GitHub
```

> **历史说明**：本仓库早期同时存在 `main` 与 `master` 两个分支（两者曾指向同一提交，`master` 是旧主线、
> `main` 是后来的主线）。为避免访问者在一个平台上看到旧代码，现已合并为单一的 `master`，两个远端的 `main`
> 都已删除。两个远端各自保存一份历史，`--force` 请谨慎使用。

## 许可与合规

**双协议**（代码与数据分开，各自用最合适的协议）：

| 范围 | 协议 | 文件 |
|---|---|---|
| 代码：`dnd/**/*.py`、`tests/`、`tools/`、`pydnd.sh`、`pyproject.toml` | **MIT** | [`LICENSE`](LICENSE) |
| 内容数据：`dnd/data/*.json`（职业/种族/怪物/物品/法术表，以及背景故事表 `lore.json`） | **CC0-1.0**（公共领域） | [`dnd/data/LICENSE-CC0.txt`](dnd/data/LICENSE-CC0.txt) |
| 文档：`README.md`、`docs/**`、检索报告 | MIT | 同 `LICENSE` |

数据表单独用 CC0 是**故意**的：它们是本项目最希望被别人替换、再创作、贴到自己项目里的部分，
CC0 取消了署名与传染义务，你可以直接改数值、加怪物、做成自己的版本。代码用 MIT 则最省事：
可 fork、可商用、可闭源再发行，只需保留版权声明。

**与原版的关系**：本项目是 1975 年 PLATO 系统上《dnd》(Gary Whisenhunt & Ray Wood) 的**非官方**现代复刻。
未使用原版 TUTOR 源码、原版数值或任何原版素材——只复刻了不受著作权保护的玩法骨架，内容表全部为本项目再创作。
本项目与原作者、PLATO/cyber1 社区及 Wizards of the Coast **均无关联，未获其授权或背书**。

**商标**：仓库内不使用 TSR/WotC 的商标与专有名词（不出现 D&D 品牌标识）；职业/种族/物品/法术与大部分怪物名
（骷髅 / 巨蛛 / 半兽人 / 食尸鬼 / 食人魔 / 吸血鬼 / 恶魂 / 炎魔）采用公有领域通用奇幻词汇，
少数怪物名为与世界观同源的自创名（灰壤鼠 / 窖犬 / 拾荒妖 / 深渊守卫），
逐条对照见 [`docs/world-setting.md`](docs/world-setting.md) 第十二节；4 职业 / 4 种族 / 12 怪物 / 18 物品 / 5 法术共 43 条内容已逐条核对。
背景故事层同理：写作前调研了「被遗忘的国度」的**结构**（失落浮空帝国 / 维系魔法的源流 / 地下幽暗世界 / 疯法师地牢），
但落地的世界名、神名、地名、事件名**全部自造**（阿瑟兰、银冠诸国、穹顶帝国、星核、织线、薇兰、深渊地牢……），
并由 `tests/test_core.py::TestLore::test_story_text_has_no_wotc_proper_nouns` 自动守线。
标题 `dnd` 属于对原作的指称性使用，不声称任何商标权。

**依赖**：游戏本体零第三方依赖（仅 Python 标准库）；`Pillow` 只被开发工具 `tools/preview.py` 使用，已标为可选依赖
（`pip install ".[preview]"`）。无网络行为、不回传统计、存档仅本地 JSON。

完整的逐项检查证据（商标扫描命令、依赖审计、凭据与隐私扫描、与 PLATO 原版的法律关系分析、
协议选型与否决理由、发布前清单）见 **[`docs/open-source-compliance.md`](docs/open-source-compliance.md)**。

## AI 参与声明（工具与来源）

> **一句话**：本仓库的代码与文档是 **AI 在 DeepSeek Harness（DSH）里多轮"调研 → 计划 → 开发 → 审查"跑出来的结果，
> 全程没有人类审查过代码或文档**。人类只做了两件事：提出需求，以及回答关键选择的提问。

### 人机边界（请务必按字面理解）

| 角色 | 实际做了什么 | 没做什么 |
|---|---|---|
| **AI**（模型 `deepseek-flash`，即 deepseek-v4.1-flash，high 思考强度） | 需求拆解、读仓库与数据表、写代码与文档、自己设计验证手段、跑测试与伪终端实验、发现并修掉自己引入的缺陷、写合规与审查报告 | —— |
| **人类** | ① 提出需求（复刻计划、启动脚本、开源合规、界面改版、文档重写等）；② 在 AI 提问时做关键选择（交付形态、协议选型、Windows 侧范围）；③ 被告知验证命令 | **没有审阅任何一行代码、没有审阅任何一份文档、没有做人工 code review，也没有逐条复核 AI 的结论** |
| **自动化流程** | 单元测试、伪终端冒烟、启动脚本自检、机器人跑批、界面预览——都由 AI 自己编写、执行、判读 | 权威评审者不是人类，所以这些验证**不能替代人工审查**；正确性请使用者自行判断 |

所以：请把它**当作"未经人工审查的 AI 产出"**来对待；要用在严肃场景，请自己安排 review。

### 本次文档重写（2026-09-11，紧随界面提交之后）

本轮只做一件事：**把全部文档按当前代码与实测结果重写一遍**，消除旧文档里已经过期的数字与说法。做法：

1. 先跑验证取得事实：`unittest`（98 项）、`tools/pty_smoke.py`（37 项）、`tools/sim.py`（四职业各 40 局）、
   `./pydnd.sh --check`、`--headless-demo`，并数清行数、内容条目与 git 历史。
2. 逐个读源码核对文档里的每条说法（入口参数、键位、存档字段、数值、布局常量、测试与工具覆盖面）。
3. 重写 README 与 `docs/` 下全部文档及根目录检索报告，改掉与实现不符的旧数字（测试数量、平衡数据、
   已做的待办项等），并补齐后来新增的开始页 / 存档管理 / Esc 菜单等内容。

### 本轮改动：序章重写（合理化）与怪物名调整（2026-09-12）

需求：**优化背景故事与生物名字；世界观向「被遗忘的国度」与主流地牢模组的结构看齐，写法要合理、
不过度玄幻，文案减轻 AI 腔。** 做法：

1. **序章重写**（`dnd/data/lore.json`）：改用主流地牢模组的开场叙事——魔法是"一门手艺"的日常视角起笔，
   断裂之夜按"法师夺权 → 织线崩断 → 浮空城坠落"的因果链平铺直叙；「遗忘之陆」的来历改为商路断绝、故地无人提起；
   深渊之门从"每隔几代人封印松动"改为**帝国陷落后封死的矮人竖井、三十年前被佣兵团炸开后一直没关**，
   井口有帐篷集市与登记册，回来的人不到十分之一；删去"梦见地底微光""硬币已经投下"一类渲染句。
   测试约束的两页结构、关键词（十层 / 深渊遗物 / 深渊守卫 / 永久）与禁用词表保持不变。
2. **怪物名**（`dnd/data/monsters.json`）：按用户指定，骷髅 / 巨蛛 / 半兽人 / 食尸鬼 / 食人魔 / 吸血鬼沿用通用名，
   幽魂改「恶魂」，巨魔改「炎魔」（火系，配色改红）；保留自创名灰壤鼠 / 窖犬 / 拾荒妖与「深渊守卫」。
   `id`、数值、字形全部不动，逐条图鉴见 [`docs/world-setting.md`](docs/world-setting.md) 第十二节。
3. **同步**：`docs/world-setting.md`（§一 / §三 / §四 / §五 / §十 对齐新叙事，§十二 图鉴逐条改写，§十一 名词对照更新）、
   `docs/how-to-play.md`（序章示意块按新文案重排、示例名更新）、`docs/open-source-compliance.md`、`tools/preview.py`。
4. **验证**：unittest 全绿；模拟跑批数值与改名前一致（名字只换标签）。

### 本轮新增：背景故事层，建角之后展示（2026-09-12）

需求：**调研 D&D 战役设定「被遗忘的国度」，据此为项目写一套背景故事，并在建角之后展示。** 做法：

1. **调研**：用 `web_search` 检索 FR 的通史与关键设定（浮空魔法帝国耐色瑞尔、魔网与魔法女神、
   卡萨斯之愚行、动荡之年、法术瘟疫、幽暗地域、Undermountain、神系分工）。
   `web_fetch` 对 wikipedia / fandom / baike 全部超时或返回 403，本机 `curl` 直连同样超时 ——
   只拿到检索元数据，所以这些结论一律按 **B 类通行说法**处理，**只用于学结构，不作为事实断言**。
   来源与"母题 → 改造"对照见 `docs/research-dossier.md` 第八节。
2. **落地**：新建 `dnd/data/lore.json`（CC0、`confidence: invented`）与 `dnd/lore.py`（不 import curses）；
   世界名、神名、地名、事件名、遗物名全部自造。`ui/screens.py` 新增 `prologue_screen`（**2 页**：
   世界 → 入井，↑↓ 滚动、←→ 翻页、回车进场、Esc 跳过）；`__main__.py` 在建角返回后调用它，
   并新增 `--no-prologue`（跳过）与 `--lore`（纯文本打印）；游戏内新增 `B` 键随时重看。
   序章**刻意只写"世界背景 + 怎么进地牢 + 下井须知"**，诸神/种族/职业/遗物细设定移出游戏，
   写进 `docs/world-setting.md`（世界观设定集）——这样进场读的是"接下来要干什么"，不是设定集。
3. **守线**：`tests/test_core.py::TestLore` 断言序章只有"世界 / 入井"两页、下井须知齐全、
   细设定（神系/种族/职业/遗物）不进游戏，并扫描故事正文禁止 WotC/TSR 的商标与专有名词；
   `tests/test_tui.py` 新增序章翻页 / 边框完整 / ASCII 降级 / `B` 键回归，以及折行"不让收尾标点跑到行首"的回归；
   `tools/pty_smoke.py` 增加"建角 → 序章 → 翻页 → 进地牢"的真实伪终端阶段。
4. **验证**：`unittest` **123 项**、`tools/pty_smoke.py` **44 项**全绿；18 个预览场景用 `tools/preview.py`
   的栅格化路径逐个跑过（0 越界告警）；Pillow 仍未安装，所以 PNG 未生成。

### 环境与配置（2026-09-11 文档重写轮实测）

| 项 | 值 |
|---|---|
| **模型** | `deepseek-flash`（配置中的显示名 `deepseek-v4.1-flash`），provider `deepseek-official` |
| **思考强度** | `reasoningEffort: high` |
| **运行框架** | DeepSeek Harness CLI `@deepseek-ai/dsh` **0.1.5-rc.1**（MIT） |
| **交互方式** | DSH Web GUI，本机 `http://127.0.0.1:3080` |
| **代理预设** | `standard` |
| **文件权限** | `workspace-write`；审批策略 `ask` |
| **常用工具** | 本会话实际使用：shell 执行、文件读写/编辑、内容检索（glob/grep）、任务清单；未使用子代理、工作流或 Ralph 循环 |

以上版本号取自本机实际安装的包（`npm ls -g --depth=0` 与 profile 的 `node_modules`），非估算。
本轮实测网络：GitHub / Gitee / npm registry 的 HTTP 均可达；`pypi.org` 在 20 秒内无响应（因此没有安装 Pillow，
`tools/preview.py` 未在本轮实跑，场景清单是从源码读出的）。

### 使用的插件

以下为当前 DSH Web 配置（profile `web`）里实际加载的插件包，只影响"代理怎么工作"，**都不是本项目的依赖**
（游戏本体依旧零第三方依赖）：

| 插件 | 版本 | 作用 |
|---|---|---|
| `@deepseek-ai/dsh-base` | 随 CLI 内置 | 核心工具集：shell、文件读写/编辑、内容搜索、任务清单 |
| `@deepseek-ai/dsh-web-app` | 随 CLI 内置 | Web 应用外壳（本次即通过它在浏览器里交互） |
| `@linxin666/dsh-web-all` | 0.3.20（Apache-2.0） | 聚合包：任务看板、Git 提交图、插件市场、模型能力、预设中心、皮肤中心、i18n 等界面类插件 |
| `dsh-anthropic-editorial` | 0.1.0（MIT） | 界面主题（以 `link:` 挂载本地目录，未发布到 npm） |

本轮实际动手只用到了 `dsh-base` 的能力；界面类插件未参与任何文件生成。

### 使用约束

本声明的语义是"这些文件由上述模型在上述环境下生成，并经过 AI 自己的实机验证，**但从未经过人类审查**"。
自动化测试由同一个模型编写，天然会漏掉它自己没想到的情况。因此采用前请自行审查
（尤其 `pydnd.sh` 这类要直接在你机器上执行、会改环境的脚本）。AI 生成不改变许可：
本仓库代码与文档仍是 MIT、内容数据仍是 CC0-1.0；同时不构成对代码正确性、安全性或适用性的担保。
